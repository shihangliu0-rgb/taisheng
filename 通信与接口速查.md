# 通信与接口速查（人和 AI 都可读）

> 一句话：主控板（STM32H7）通过 **4 路串口 + 1 路 FDCAN + 抬升 CAN** 与外界通信；
> 谁在哪个任务里、用哪个函数、收发了什么帧，本页一次说清。
> 详细协议见文末「对应文档」列（仓库内已有文档，本页不重复粘贴）。

---

## 0. 数据流总览

```
                      ┌────────────────────────────────────────────────┐
  操作手上位机(无线)    │  主控板 STM32H7 (freertos.c 三个任务)            │
  ── UART4 ──────────►│                                                │
    速度/动作/急停/模式 │  commTask: DT35PnpLink_Run → Action_UpdatePnp    │
  ◄─ yaw+DT35+PNP ──── │             → ComputerLink_Run → PcLink_Run      │
                       │             → PathRunner_Arbitrate → PathRunner_Run│
  小电脑(ROS2)         │                                                │
  ── UART7 ──────────►│  chassisTask: ImuMain_Run1ms → Chassis_Run1ms    │
    0x11 位姿 / 0x10 感知│  liftTask:    Action_Run1ms → Up_Run1ms         │
  ◄─ 0x20 状态帧 ───── │                                                │
                       └───────┬──────────────────────┬─────────────────┘
  IMU(DM-IMU L1)                │ FDCAN1               │ 抬升 CAN
  ── UART1 ──► ImuMain_Run1ms   └─► 4×VESC(底盘)       └─► RS00/DM/M2006
  DT35前/左 + PNP
  ── UART9 ──► DT35PnpLink_Run
```

---

## 1. 物理链路分配（串口 / CAN 占用）

| 资源 | 设备 | 波特率 | 模块/初始化入口 | 方向 |
|---|---|---|---|---|
| `huart1` (USART1) | DM-IMU L1 陀螺仪 | 921600 | `ImuMain_Init()` → `Imu_Init(&huart1)` | 收（姿态主动上报） |
| `huart4` (UART4) | 操作手端上位机（无线 DAPLink 透明串口） | 115200 | `ComputerLink_Init(&huart4)` | 收指令 / 发遥测 |
| `huart7` (UART7) | 小电脑 ROS2 competition_gateway | 115200 | `PcLink_Init()` | 收位姿/感知 / 发状态 |
| `huart8` (UART8) | 调试串口（可选） | 2000000 | `PATH_DEBUG_UART_HANDLE` | 发调试日志 |
| `huart9` (UART9) | DT35 前/左激光 + PNP 传感器 | — | `DT35PnpLink_Init(&huart9)` | 收测距/触发 |
| `huart2` (UART2) | 预留遥控器（未启用，勿动） | — | — | — |
| FDCAN1 | 底盘 4×VESC | — | `Chassis_Init()` → `VescCan_Init(&vesc_bus, &hfdcan1)` | 双向 |
| 抬升 CAN | RS00 / DM / M2006 | — | `Up_Init()` | 双向 |

---

## 2. 每条链路：谁 ↔ 谁、哪个函数、什么帧

### 2.1 IMU（`huart1`）—— 底盘航向与角速度来源

| 项 | 内容 |
|---|---|
| 接收入口 | `ImuMain_Run1ms()`（chassisTask，每 1ms）；中断 `ImuMain_HandleRxEvent()` |
| 数据产出 | `imu_data_t`：`yaw_deg`、`gyro_z_deg_s`、`state`、`online`、`yaw_valid`、`gyro_valid` |
| 取数函数 | `ImuMain_GetData(&imu)`（path_main.c 融合、CALIB 标定、故障判据都用它） |
| 航向闭环 | `ImuMain_CalcOmega(vx, vy, z)`（chassis_main.c 的 `Chassis_Run1ms` 调用，做 yaw-hold） |
| 回传 | `ImuMain_SendYaw(uart)`：每 50ms 发 `A5 5C | yaw_cdeg(le16) | XOR(2-3)` |
| 说明 | 启动时序（校准→配置→零偏采样）见 `user/imu/imu_main.c` 内部状态机 |

### 2.2 DT35 + PNP（`huart9`）—— 前/左激光测距 + 触发

| 项 | 内容 |
|---|---|
| 接收入口 | `DT35PnpLink_Run()`（commTask，离线检测）+ 中断 `DT35PnpLink_RxCplt()` |
| 数据产出 | `dt35_link[2]`（`distance_cm` / `online` / `last_rx_ms`）、`pnp_link[2]`（`trigger` / `online`） |
| **激光测距消费者** | `runner_read_and_fuse()`（path_main.c）读 `dt35_link[SENSOR_LINK_F_INDEX].distance_cm`（前）与 `[SENSOR_LINK_L_B_INDEX]`（左），做 **3 点中值滤波** → 前激光兜底减速/急停、侧向微调 |
| **PNP 消费者** | `Action_UpdatePnp(pnp_link[F].trigger, pnp_link[L_B].trigger)`（freertos.c）→ 抬升动作触发 |
| 转发遥测 | `DT35PnpLink_Send(uart)`：有变化才发，回给操作手端 |
| 帧格式 | DT35 `AA | addr | dist_lo | dist_hi | XOR(0-3)`；PNP `AB | addr | trigger | 0 | XOR(0-3)`；地址 F=0x40、L=0x41 |

### 2.3 操作手端上位机（`huart4`）—— 速度/动作/急停/模式

| 项 | 内容 |
|---|---|
| 接收入口 | `ComputerLink_Run()` + 中断 `ComputerLink_RxCplt()` |
| 收帧 | `A5 5A` 速度(9B，vx/vy/z le16 + XOR)、`A5 5B` 动作(3B)、`A5 5D` 急停(3B：01 锁存/00 清除)、`A5 5E` 模式(3B：01 手动/00 自主) |
| 消费者 | `PathRunner_Arbitrate()`：按 **急停 > 人工 > 自主** 仲裁 → `Chassis_SetVelocity()` / `Action_Request()` / 状态机切换 |
| 回传 | 同函数内 `ImuMain_SendYaw()` + `DT35PnpLink_Send()` |
| 注意 | `A5 5D`/`A5 5E` 为本轮新增，操作手端 `uper_computer/robot_protocol.py` 尚未实现，需补 |

### 2.4 小电脑 ROS2（`huart7`）—— 场地位姿与感知

| 项 | 内容 |
|---|---|
| 接收入口 | `PcLink_Run()` + 中断 `PcLink_RxCplt()` |
| 收帧 | `AA 55 11` 位置帧 24B（`field_x/y/z_m`、`field_w`(rad, yaw=0 朝 +y)、seq、flags、累加和、`0D 0A`）；`AA 55 10` 感知帧 44B（红/蓝/球 xyz） |
| 消费者 | path_main.c：`PcLink_GetPositionSeq()` 判新帧 → `PcLink_GetPosition(&upper)` → `PathFusion_UpdateUpper()` 融合位姿 |
| 回传 | `PcLink_SetStatus(state, error)`：每 50ms 发 `55 AA 20 | state | error | 累加和 | 0D 0A`（8B） |
| 统计 | `PcLink_GetStats(&frames, &crc_errors)` |

### 2.5 底盘 VESC（FDCAN1）—— 唯一底盘执行器

| 项 | 内容 |
|---|---|
| 初始化 | `Chassis_Init()` → `VescCan_Init(&vesc_bus, &hfdcan1)` + `VescMotor_Init()` ×4 |
| 周期 | `Chassis_Run1ms()`：解析反馈 + 每 10ms 下发目标 |
| 写 | `Chassis_SetVelocity(vx, vy, z)`（轮 RPM / 控制量；会被 `PathRunner_Arbitrate` 或 RUN 的 `runner_step` 调用） |
| 停 | `Chassis_StopAll()`：`SET_BRAKE` 18A 锁存（故障安全，需 `Chassis_SetVelocity` 解除） |
| 读 | `Chassis_GetStatus()`；`motors_online()`（path_main.c 电机在线判据） |
| ID | 左前 87、右前 43、左后 67、右后 59 |

### 2.6 抬升机构（RS00 / DM / M2006，CAN）—— 动作执行

| 项 | 内容 |
|---|---|
| 周期 | `Up_Run1ms()`（liftTask）+ `Action_Run1ms()` |
| 位置指令 | `Up_SetRsPos(deg)`（两台 RS00）、`Up_SetDmPos(deg)`（两台达妙）、`Up_MoveM2006(offset_deg)`、`Up_SetMotorPos(...)`（四台联合） |
| 状态 | `Up_MotorMoveDone()`、`Up_GetRsStatus()`、`Up_GetDmStatus()`；`Up_StopAll()` |
| 动作层 | `Action_Request(cmd)` / `Action_Run1ms()` / `Action_UpdatePnp()`（见 action_api.c） |

### 2.7 调试串口（`huart8`，可选）

`PathRunner_DebugDump()` 每 400ms 输出一行运行状态（state/reason/位姿/指令/激光等），仅在 `PATH_DEBUG` + `PATH_DEBUG_UART_HANDLE` 定义时编译。

---

## 3. 任务调度（freertos.c）—— 谁在哪个任务里跑

| 任务 | 周期 | 调用序列 |
|---|---|---|
| `chassisTask` | 1ms | `ImuMain_Run1ms()` → `Chassis_Run1ms()` |
| `liftTask` | 1ms | `Action_Run1ms()` → `Up_Run1ms()` |
| `commTask` | 1ms | `DT35PnpLink_Run()` → `Action_UpdatePnp()` → `ComputerLink_Run()` → `PcLink_Run()` → `PathRunner_Arbitrate()`（1ms，仲裁写底盘） → `PathRunner_Run()`（5ms 分频，自主状态机） |

---

## 4. 帧格式速查（汇总）

| 链路 | 帧 | 布局 | 校验 |
|---|---|---|---|
| UART4 收 | 速度 | `A5 5A` + vx,vy,z(le16) | 字节 2-7 异或 |
| UART4 收 | 动作 | `A5 5B` + action | 无 |
| UART4 收 | 急停(新) | `A5 5D` + sub(01/00) | 无 |
| UART4 收 | 模式(新) | `A5 5E` + mode(01/00) | 无 |
| UART4 发 | yaw | `A5 5C` + yaw_cdeg(le16) | 字节 2-3 异或 |
| UART4 发 | DT35 | `AA` + addr + dist(le16) | 字节 0-3 异或 |
| UART4 发 | PNP | `AB` + addr + trigger + 0 | 字节 0-3 异或 |
| UART7 收 | 位置 | `AA 55 11` + seq + flags + field_xyz + field_w + chk + `0D 0A`（24B） | 8 位累加和 |
| UART7 收 | 感知 | `AA 55 10` + seq + flags + 9×float + chk + `0D 0A`（44B） | 8 位累加和 |
| UART7 发 | 状态 | `55 AA 20` + state + error + chk + `0D 0A`（8B） | 8 位累加和 |

> 底盘 VESC 走 FDCAN1（`vesc_can.c`），抬升走 RS00/DM/M2006 CAN（`rs_bus.c`/`dm_2006_bus.c`/`c610_2006.c`），非串口。

---

## 5. 「哪个函数算出什么」索引

| 想知道 | 看这里 |
|---|---|
| 前/左激光距离怎么来的 | `dt35_link[].distance_cm` ← `DT35PnpLink_RxCplt` 解析 → `runner_read_and_fuse()` 3 点中值滤波 |
| 位姿怎么融合出来的 | `PcLink_GetPosition()`(小电脑位姿) + `ImuMain_GetData()`(gyro) → `PathFusion_UpdateUpper/Predict`（path_main.c） |
| 底盘速度指令怎么算出来的 | `runner_step()`：查表 + 纯追踪 + 前激光兜底 + yaw-lock → `Chassis_SetVelocity()` |
| 故障/状态怎么上报的 | `PcLink_SetStatus(state, error)` → `55 AA 20` 状态帧 |
| 人工/急停谁裁决 | `PathRunner_Arbitrate()`（急停 > 人工 > 自主） |

---

## 6. 对应文档（详细协议见这些）

| 文档 | 内容 |
|---|---|
| `uper_computer/遥测.md` | 操作手端上位机 ↔ 机器人完整串口协议（速度/动作/yaw/DT35/PNP 字段级定义） |
| `uper_computer/README.md` | 上位机按键映射、速度范围、保活规则 |
| `uper_computer/robot_protocol.py` | 上位机侧帧打包/解析实现（Python 参考） |
| `user/pc_link/README.md` + `pc_link.h` 头注释 | 小电脑 ROS2 0x11/0x10/0x20 帧级协议 |
| `user/path_planner/README.md` | 路径规划数据流、状态机、停机码、安全规则 |
| `user/chassis_vesc/*`、`user/up/*`、`user/2006/*` | VESC / RS00 / DM / M2006 电机层协议与状态 |
