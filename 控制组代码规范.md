# 控制组代码规范 V1.0

本规范专门针对 **Robocon 控制组（STM32 + HAL + VESC/CAN）** 制定，不追求复杂的软件工程规则，核心目标只有三个：

1. 比赛期间每个人都能看懂。
2. 代码出问题时能快速定位。
3. 方便多人协作，也方便 AI 辅助开发与维护。

---

## 一、命名规范

### 1. 变量

统一采用 **小写 + 下划线**：

```c
float target_speed;
float current_speed;
uint8_t motor_state;
```

不要这样写：

```c
float TargetSpeed;
float a;
float temp1;
```

### 2. 宏定义

全部大写：

```c
#define MAX_SPEED      5.0f
#define WHEEL_RADIUS   0.08f
#define PI             3.1415926f
```

### 3. 枚举

枚举类型：

```c
typedef enum
{
    MOTOR_STOP,
    MOTOR_RUN,
    MOTOR_ERROR
} motor_state_t;
```

变量：

```c
motor_state_t motor_state;
```

### 4. 结构体

```c
typedef struct
{
    float speed;
    float angle;
    float current;
} motor_data_t;
```

统一加 `_t` 后缀。

### 5. 函数命名

统一采用：

`模块_动作()`

例如：

```c
Motor_Init();
Motor_SetSpeed();
Motor_Stop();
Can_Send();
Can_Receive();
Imu_Update();
Vision_Parse();
```

不要这样写：

```c
Init();
Run();
test();
abc();
```

### 6. 命名长度

变量名和函数名在含义清楚的前提下应尽量简短，建议不超过 **20 个字符或 4 个英文单词**；协议名、硬件型号等固定名称可以例外。

可以统一使用团队内含义明确的常用缩写：

```text
velocity -> vel
position -> pos
command  -> cmd
config   -> cfg
current  -> cur
previous -> prev
maximum  -> max
minimum  -> min
```

单位后缀不能为了缩短命名而省略，例如 `_m_s`、`_rad_s`、`_ms` 必须保留。不要自造生僻缩写，也不要把变量缩成无法理解的单字母。

推荐：

```c
float target_vel_m_s;
float cur_yaw_rad;
void Chassis_GetKeyVel(void);
```

不推荐：

```c
float target_linear_velocity_meter_per_second;
void Chassis_CalculateKeyboardVelocityCommand(void);
```

---

## 二、文件组织

按模块拆分文件，例如：

电机：

```text
Motor.c
Motor.h
```

IMU：

```text
Imu.c
Imu.h
```

CAN：

```text
Can.c
Can.h
```

底盘：

```text
Chassis.c
Chassis.h
```

视觉：

```text
Vision.c
Vision.h
```

每个模块只负责自己的事情。

例如：

- `Motor.c` 不要去解析视觉。
- `Vision.c` 不要去控制 PWM。

---

## 三、每个 `.c` 文件格式

统一按如下顺序组织：

```c
#include "Motor.h"

#define ...

typedef ...

static ...

全局变量

函数
```

例如：

```c
#include "Motor.h"

#define CURRENT_LIMIT 10

static float motor_offset;

motor_data_t motor_data;

void Motor_Init(void)
{

}

void Motor_Update(void)
{

}
```

---

## 四、变量作用域

控制量、状态量和调试量优先使用含义明确的普通全局变量，便于在调试器中直接观察和修改：

```c
float target_speed;
float current_speed;
float speed_pid_output;
uint8_t motor_enable;
```

只在单个 `.c` 文件内使用、且不需要从调试器或其他模块访问的变量，可以加 `static`：

```c
static float imu_bias;
```

多个文件确实需要共享时，在对应头文件中使用 `extern` 声明：

```c
extern float target_speed;
extern uint8_t motor_enable;
```

不要为了“封装”把常用变量藏进多层结构体，再通过上下文指针层层传递：

```c
robot.ctrl.chassis.motor[0].pid.output  // 不推荐
ctx->chassis->motor->target_speed      // 不推荐
```

全局变量也不能无序堆放。变量应在所属模块中定义，命名带模块或功能含义；禁止不同模块定义同名、含义不明的 `data`、`temp`、`flag`。

---

## 五、注释规范

不要写：

```c
//速度
```

要写：

```c
// 根据目标速度计算电流输出
```

函数统一写标准注释：

```c
/**
 * @brief 电机速度控制
 * @param target_speed 目标速度(rad/s)
 * @retval None
 */
```

---

## 六、`if` 格式

统一写法：

```c
if (speed > MAX_SPEED)
{
    ...
}
else
{
    ...
}
```

不要这样写：

```c
if(speed>MAX_SPEED){
...
}
```

---

## 七、`switch` 规范

每个 `case` 必须 `break`：

```c
switch (state)
{
case MOTOR_STOP:
    ...
    break;

case MOTOR_RUN:
    ...
    break;

default:
    break;
}
```

---

## 八、魔法数字与参数管理

魔法数字处理不能走两个极端：既不能在逻辑中散落一堆含义不明的数字，也不能把每个局部临时值都定义成宏。

### 1. 必须提取的数字

以下数字应使用有含义的宏，并写明单位：

- 需要现场调节或可能修改的控制参数。
- 在两个及以上位置重复使用的参数。
- 协议 ID、寄存器位、硬件限值和机械尺寸。
- 不看说明就无法理解含义的阈值、周期和换算系数。

```c
#define RAD_TO_DEG          57.2958f
#define VISION_PERIOD_MS    20U
#define YAW_THRESHOLD_RAD   0.15f
#define MOTOR_MAX_CUR_A     10.0f
```

### 2. 可以直接写的数字

以下数字不需要为了“消除魔法数字”强行定义宏：

- `0`、`1`、`-1` 等含义明确的状态判断或简单运算。
- 只使用一次、含义在当前语句中一眼可见的局部值。
- 数组下标、循环步进和协议固定字节位置；协议复杂时除外。
- 临时调试参数。调试结束后必须删除或转为正式参数。

```c
motor_enable = 0;

for (uint8_t i = 0; i < 4; i++)
{
    motor_current[i] = 0.0f;
}
```

不要这样定义：

```c
#define LOOP_START          0
#define LOOP_STEP           1
#define MOTOR_DISABLE       0
#define TEMP_TEST_VALUE     3
```

### 3. 参数放置位置

- 仅当前模块使用的参数，放在对应 `.c` 文件顶部，不要塞进公共头文件。
- 多个模块共用的硬件参数或协议常量，才放入对应头文件。
- 禁止建立包含全项目所有参数的巨大 `config.h`。
- 禁止为了参数“可配置”增加运行时配置系统；比赛参数在编译期直接确定。

判断标准：提取后能提高含义表达、统一修改或防止误改，就提取；只会增加跳转查找成本，就保留在原处。

---

## 九、比赛嵌入式轻量化与调试规范

本项目面向资源有限的单片机和比赛现场调试，不照搬大型软件工程架构。代码优先保证执行路径短、变量可见、断点好打、出问题时能快速定位。

### 1. 禁止过度设计

禁止以下做法：

- 为裸机项目引入面向对象式框架、虚函数、复杂继承或多态设计。
- 为简单 PID、滤波或状态判断设计多层结构体和上下文指针。
- 使用多层回调或函数指针数组分发正常控制流程。
- 为“以后可能扩展”预留当前没有实际用途的接口、字段和适配层。
- 在 HAL 外再包一层通用硬件抽象层或“可移植层”。
- 为了形式上的解耦，把一段连续逻辑拆成大量只调用下一层的小函数。

只有当某段代码确实重复、职责独立，或能明显降低复杂度时才封装。

### 2. 调用层级

- 从主循环或中断入口算起，完成一个控制动作的自有函数调用原则上不超过三层；HAL 库内部调用不计入。
- 一段短小、只使用一次的流程可以直接展开写，便于逐行单步调试。
- 不要写只转发参数、不增加实际逻辑的“套娃函数”。
- 共用算法、重复通信操作和明确的硬件动作仍应正常封装，不能复制粘贴多份实现。

推荐：

```text
主循环 -> Chassis_Update() -> HAL_CAN_AddTxMessage()
```

不推荐：

```text
主循环 -> Robot_Update() -> App_Update() -> Chassis_Update()
       -> Motor_Update() -> Motor_Send() -> Can_Send() -> HAL_CAN_AddTxMessage()
```

### 3. 数据组织与调试可见性

- 目标值、反馈值、输出值、状态和故障标志使用普通变量，允许直接全局访问。
- 不把调试时常看的变量藏在深层结构体、动态内存或只能通过 getter 读取的位置。
- 不使用结构体加指针传参来包装简单控制流程。
- 禁止动态内存分配；数组大小和资源数量在编译期确定。
- 变量必须归属明确，不能用一个巨大结构体或无规则的全局变量堆保存整个系统状态。
- 只有中断与主循环共享的标志、硬件寄存器映射等确实需要的对象才加 `volatile`，不要为了方便观察给所有变量加 `volatile`。

### 4. 中断处理

中断服务函数原则上只清除必要的硬件状态并置标志位；数据解析、控制计算和业务处理全部放到主循环执行。DMA 或 HAL 已经接收好的数据直接留在固定缓冲区，不在中断中再次搬运。

```c
volatile uint8_t imu_data_ready;

void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    imu_data_ready = 1;
}
```

主循环：

```c
if (imu_data_ready != 0)
{
    imu_data_ready = 0;
    Imu_Parse();
}
```

禁止为了传递普通中断事件引入队列、消息总线或任务调度框架。若数据可能在主循环处理前被多次覆盖，使用计数标志或固定长度缓冲区，并明确溢出处理。

### 5. 延时与调度

- 简单阻塞延时直接使用 `HAL_Delay()`。
- 固定周期任务使用现有定时器中断置标志位，由主循环执行。
- 不自行设计通用定时器调度器、软件任务系统或时间轮。
- 控制闭环和中断中不得使用 `HAL_Delay()`。

### 6. 错误处理

- 当前函数发现前置条件不满足或 HAL 调用失败时，立即置必要的故障标志并 `return`。
- 不为了“完整错误链”设计多层错误码、异常对象或统一错误传播框架。
- 涉及电机失控、通信超时等安全问题时，必须先关闭输出或进入安全状态，不能只返回。

```c
if (HAL_CAN_AddTxMessage(&hcan1, &tx_header, tx_data, &mailbox) != HAL_OK)
{
    can_send_error = 1;
    return;
}
```

### 7. 硬件配置

- 外设配置直接使用 STM32CubeMX 生成的 HAL 初始化代码、HAL 接口或必要的寄存器操作。
- 配置在初始化阶段一次完成，不允许运行时动态创建、切换或注册驱动。
- 必须写清直接寄存器操作的目的和涉及的位，避免无说明的十六进制常量。
- 不为不同芯片或不同 HAL 预先设计当前项目用不到的兼容接口。

### 8. 采用复杂方案的例外

队列、回调、复杂状态保存或额外抽象只有在现有简单方案已无法满足需求时才能使用。使用前必须说明具体问题，例如数据确实会丢失、存在多个同类设备或实测时序不满足；不能只以“更优雅”或“方便扩展”为理由。

---

## 十、PID 规范

PID 参数和中间量直接使用命名清楚的普通变量，便于调试器观察：

```c
float speed_pid_kp;
float speed_pid_ki;
float speed_pid_kd;
float speed_pid_error;
float speed_pid_integral;
float speed_pid_output;
```

简单 PID 可以在所属模块内直接计算，或封装为一个不再套娃的 `SpeedPid_Calc()` 函数。不要为了复用建立虚函数、深层结构体、复杂 PID 管理器或通过 `pid_t *` 隐藏所有中间量。

命名必须体现控制对象，例如：

```c
SpeedPid_Calc(void);
YawPid_Calc(void);
AnglePid_Calc(void);
```

不要这样写：

```c
PID2(void);
PID_new(void);
Algorithm_Controller_Run(void);
```

---

## 十一、CAN 规范

所有 ID 统一放在一个文件：

```text
can_id.h
```

例如：

```c
#define CAN_VESC1_ID      0x01
#define CAN_VESC2_ID      0x02
#define CAN_IMU_ID        0x11
#define CAN_VISION_ID     0x21
```

不要到处散落：

```text
0x201
0x321
0x835
```

以后改 ID 很方便。

---

## 十二、状态机

禁止大量分散的条件判断：

```c
if (...)
{
    ...
}

if (...)
{
    ...
}

if (...)
{
    ...
}
```

统一写成状态机：

```c
typedef enum
{
    ROBOT_INIT,
    ROBOT_READY,
    ROBOT_RUN,
    ROBOT_ERROR
} robot_state_t;
```

然后：

```c
switch (robot.state)
{
    ...
}
```

可维护性高很多。

---

## 十三、控制周期

例如 `1kHz` 控制周期，统一执行顺序：

```text
读取传感器
-> 状态更新
-> PID
-> 运动学
-> CAN发送
```

执行顺序固定，不允许有人随便改。

---

## 十四、日志输出

禁止直接到处写：

```c
printf("%f", speed);
```

统一使用一层可直接关闭的日志宏：

```c
LOG_INFO("speed=%.2f", speed);
LOG_ERROR("CAN timeout");
LOG_WARN("IMU drift");
```

后期关闭日志只改一个宏。不要再增加日志对象、队列、后端注册或运行时分级系统。

---



## 十五、代码职责与调用层级

为了避免功能越写越乱，控制组按下列类别划分代码职责。这里的分层主要用于确定代码放在哪里，不代表一次调用必须逐层转发；实际调用仍须遵守第九节的三层限制。

推荐分层如下：

```text
硬件层        BSP / HAL
驱动层        CAN / IMU / Encoder / VESC
算法层        PID / Kalman / Kinematics / Filter
业务层        Chassis / Gimbal / Shoot / Vision
应用层        Robot / Task / State Machine
调试层        UI / Tool / Python / Log / Auto Test
```

各层职责如下：

### 1. 硬件层

负责最底层硬件初始化和片上资源配置，例如：

- GPIO
- TIM
- UART
- SPI
- CAN
- DMA

这一层只负责“硬件能工作”，不负责机器人逻辑。

### 2. 驱动层

负责具体设备的数据收发和基础封装，例如：

- IMU 数据读取
- 编码器数据读取
- VESC 报文发送与接收
- CAN 数据打包与解析

这一层只提供接口，不直接参与底盘、云台、发射等控制决策。

例如：

- `Vesc_SetCurrent()`
- `Imu_GetAngle()`
- `Can_SendFrame()`

### 3. 算法层

负责纯算法计算，不直接操作硬件。

例如：

- PID
- 一阶滤波
- 卡尔曼滤波
- 正逆运动学
- 限幅与斜坡

算法层输入数据，输出结果，不关心数据从哪里来，也不关心结果发到哪里去。

### 4. 业务层

负责单个功能模块的控制逻辑，例如：

- 底盘控制
- 云台控制
- 发射机构控制
- 视觉数据处理

这一层可以调用驱动层和算法层，但不要直接碰底层寄存器，也不要把系统总状态全写在里面。

### 5. 应用层

负责整机调度与状态切换，例如：

- 机器人总状态机
- 不同任务模式切换
- 周期调度
- 模块使能关系管理

这一层决定“什么时候调用谁”，但不负责具体算法实现。

### 6. 调试层

这一层独立于板端控制代码，主要负责调试、配置、测试和辅助开发，例如：

- 上位机界面
- 串口 / CAN 调试工具
- Python 测试脚本
- 日志解析脚本
- 自动发包与回包验证脚本

这一层可以读取数据、发送命令、记录日志、辅助标定，但不直接参与单片机内部控制流程。

例如：

- 发送目标速度进行联调
- 读取 IMU、VESC、底盘状态做可视化
- 批量测试通信是否超时
- 自动保存比赛日志用于复盘

上位机和脚本代码也要按模块拆分，不要把“界面、协议、解析、控制逻辑、日志处理”全写进一个文件。

### 7. 调用关系

依赖方向原则上遵循：

```text
调试层
  ↓
应用层
  ↓
业务层
  ↓
算法层 / 驱动层
  ↓
硬件层
```

禁止以下职责混乱：

- `Vision.c` 直接去改 GPIO
- `PID.c` 里面直接发 CAN
- `Motor.c` 里面直接解析裁判系统或视觉协议
- 应用层跨过业务层直接把零散控制逻辑写进任务函数
- Python 脚本直接替代板端状态机长期控制机器人
- 上位机协议解析和板端驱动细节混写在同一个模块里

允许上层在职责清楚时直接调用算法、驱动或 HAL 接口，不得为了凑齐层级增加空转发函数。

说明：

- 上位机/脚本层可以和应用层通信，但不能反过来把板端核心逻辑依赖在脚本上。
- 比赛时机器人必须在脱离上位机后也能独立运行。
- 脚本适合做测试、标定、记录和辅助分析，不适合承载核心闭环控制。

### 8. 分层原则

代码分层的目的不是为了好看，而是为了：

1. 改驱动时不影响上层逻辑。
2. 改算法时不需要改底层通信。
3. 比赛现场出问题时能快速判断是哪一层出了问题。
4. AI 修改代码时更不容易误伤其他模块。

---

## 十七、AI 协作规范（强烈推荐）

由于控制组会大量使用 AI（如 ChatGPT、Codex 等）辅助开发，建议统一遵循以下原则：

1. 一个提示词只完成一个功能。
2. 每次只修改一个模块，避免 AI 改动无关代码。
3. 提交前必须人工阅读，确认命名、注释、逻辑符合团队规范。
4. 先在分支验证，再合并到主分支，不要直接覆盖稳定代码。
5. 重要算法必须保留原理注释，不能只留下 AI 生成结果。
6. 明确要求 AI 优先使用全局普通变量、直接控制流程和 HAL，不得主动增加结构体上下文、回调分发、队列、调度器或可移植层。
7. AI 给出复杂方案时，必须说明简单方案为什么不能满足需求；没有实测依据时采用简单方案。

例如：

- 可以说“实现 CAN 接收解析”
- 不要同时要求“重构底盘、修改 PID、增加日志”

---

## 十八、团队原则（最重要）

> 代码是给队友看的，其次才是给电脑运行的。

优秀的比赛代码应该做到：

1. 统一命名，不用猜变量含义。
2. 统一格式，任何成员打开都能快速定位。
3. 职责清楚，修改一个功能时容易找到相关代码。
4. 参数适度集中，避免数字散落，也避免宏定义泛滥。
5. 可调试、可维护、可复用，比赛现场出现问题能迅速定位和修复。

对于 Robocon 控制组来说，最合适的代码不是抽象最多的代码，而是满足当前需求、执行路径清楚、单步调试顺畅、比赛现场能快速修改的代码。
